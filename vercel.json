{
  "installCommand": "corepack enable && corepack prepare pnpm@latest --activate && pnpm install && pnpm run db-build",
  "crons": [
    {
      "path": "/api/check-weekly-winner",
      "schedule": "0 8 1 * *"
    }
  ]
}
